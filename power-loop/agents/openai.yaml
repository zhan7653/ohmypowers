interface:
  display_name: "Power Loop"
  short_description: "Plan lean, review-gated Codex loops"
  default_prompt: "Use $power-loop on this persisted Task Contract. Verify readiness and the recorded delivery lane. Prefer direct execution for LIGHT work; generate a compact confirmed Execution Blueprint only for STANDARD/HIGH or explicit handoff needs."
