interface:
  display_name: "Power Loop"
  short_description: "Plan and dispatch cost-aware Codex loops"
  default_prompt: "Use $power-loop on this persisted task contract to generate a Blueprint, Dispatch Plan, and confirmable Issue Patch before a Goal Prompt."
