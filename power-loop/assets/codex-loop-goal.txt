/goal Start the confirmed work from the sole normative contract at <contract-source>.

Pinned Issue identity:
- Source: <contract-source>
- Host revision: <host revision metadata when available, or unavailable>
- Exact complete-body SHA-256: <sha256>
- Exact Task Contract SHA-256: <sha256 of UTF-8 bytes from document start to the byte before the Blueprint start marker>

Required confirmed references:
- Task Contract: <heading or anchor>
- Execution Blueprint: <heading or anchor; must say Planning status: confirmed>
- Agent Dispatch Plan: <heading or anchor; must say Planning status: confirmed>

Preflight:
1. Re-read the exact complete persisted body from Source. Treat it as the sole normative contract; this Goal is supplementary execution evidence and adds no obligations.
2. Hash the exact body bytes without normalization and require the complete-body SHA-256 above to match. The body digest is authoritative; stop on a mismatch even if Host revision is unchanged.
3. Hash the exact Task Contract byte range and require its SHA-256 above to match. Require both referenced planning sections to be present and confirmed.
4. Verify the repository baseline and current subagent capability against the referenced confirmed sections. Do not launch a probe when its visible schema is conclusive. `strict-model-routing` and `inherited-model-routing` claims remain limited to the capability evidence persisted there. Instruction-level no-write behavior is not host-enforced isolation.
5. Stop before implementation on any Issue identity, Task Contract, planning-status, repository-baseline, execution-mode, capability, section, or source-access drift. Use the Issue's own pause/replan rules; do not repair drift by changing this Goal.

If preflight passes, follow the referenced confirmed Issue sections exactly. The user must start this Goal manually; do not invoke it automatically.
