/goal Start the confirmed work from the sole normative Task Contract at <contract-source>.

Pinned Issue identity:
- Source: <contract-source>
- Host revision: <metadata or unavailable>
- Exact complete-body SHA-256: <sha256>
- Exact Task Contract SHA-256: <sha256>

Required confirmed reference:
- Execution Blueprint reference: <heading or anchor; Planning status: confirmed>
- Execution Blueprint artifact: <source and exact SHA-256>

Preflight:
1. Re-read the complete persisted body and treat only its Task Contract byte range as normative.
2. Require the complete-body and Task Contract digests above to match exact bytes.
3. Require the compact Blueprint reference and Blueprint artifact digest to match.
4. Verify the repository baseline and stop on material Task Contract, identity, Blueprint, source-access, or repository drift.

If preflight passes, follow the confirmed Blueprint. Keep exploration, implementation, tests, integration, validation, documentation, evidence packaging, and repair in the main agent.

After the final tree is frozen and V2/V3 evidence exists, inspect the runtime reviewer-spawn capability, create a Final Review Plan from `power-loop/assets/final-review-plan.md`, launch the decoupled final reviewer wave, run `power-verifier`, and prepare compact PR/MR evidence.

Do not require or confirm reviewer routing before implementation. Ask for a human decision only when an exact Task Contract reviewer/model/profile/provider/reasoning/sandbox/isolation requirement cannot be satisfied.

The user must start this Goal manually; do not invoke it automatically.
