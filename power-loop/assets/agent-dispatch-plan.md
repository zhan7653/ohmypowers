# Agent Dispatch Plan Template Router

Do not fill this file as an execution plan.

After capability preflight and explicit user confirmation, use exactly one template:

- `strict-model-routing` -> `agent-dispatch-plan-strict.md`
- `inherited-model-routing` -> `agent-dispatch-plan-inherited.md`

Record the confirmed mode and capability evidence in the selected template. Do not combine the two templates or generate mode-specific planning before confirmation.
