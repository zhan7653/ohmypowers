{
  "name": "power-work-report",
  "version": "0.1.0",
  "description": "Manual Codex daily work report generator for ohmypowers.",
  "type": "module",
  "bin": {
    "power-work-report": "bin/power-work-report.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check bin/power-work-report.js && node --check lib/*.js && node --check test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
