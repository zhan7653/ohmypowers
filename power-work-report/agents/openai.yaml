interface:
  display_name: "Power Work Report"
  short_description: "Review daily work, personal reflection, and reusable insights"
  default_prompt: "Use $power-work-report to generate today's draft, pause for the optional personal memo checkpoint, review insight safety signals, and keep finalize separate from instruction-plan and the second-confirmed compare-and-commit instruction-apply."
