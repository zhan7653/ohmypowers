interface:
  display_name: "Power Verifier"
  short_description: "Verify implementation evidence against a canonical Issue contract"
  default_prompt: "Use $power-verifier for a compact read-only conformance review against the canonical Task Contract and final frozen tree. Report exceptions instead of duplicating passing evidence or reviewer telemetry."
