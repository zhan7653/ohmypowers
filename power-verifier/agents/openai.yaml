interface:
  display_name: "Power Verifier"
  short_description: "Verify implementation evidence against a Loop Engineering contract"
  default_prompt: "Use $power-verifier to check this issue contract, diff, validation output, and PR evidence."
