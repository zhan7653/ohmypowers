interface:
  display_name: "Power Verifier"
  short_description: "Verify implementation evidence against an Issue and Goal contract"
  default_prompt: "Use $power-verifier for a read-only, snapshot-bound contract-conformance review of this Issue/local contract, final Goal Prompt, implementation evidence, validation replay, and review provenance."
